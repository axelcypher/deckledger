services:
  deckledger:
    build: .
    container_name: deckledger
    restart: unless-stopped
    ports:
      - "18081:8080"
    environment:
      DATABASE_PATH: /data/deckledger.db
      SECRET_KEY: ${SECRET_KEY:-deckledger-local-development-key}
    volumes:
      - deckledger_data:/data
      - ./public:/app/public:ro

volumes:
  deckledger_data:
